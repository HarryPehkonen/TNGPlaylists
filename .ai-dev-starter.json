{
  "kit": "AI-DEV-STARTER",
  "url": "https://github.com/HarryPehkonen/AI-DEV-STARTER",
  "revision": "951608c2675a2f5c2d0b59d9c19c632d23a043af",
  "recorded_at": "2026-09-20",
  "record_kind": "retroactive",
  "record_note": "Wired by hand on 2026-09-19 (commit 3b92406 'quality baseline: gate, contract tests, version pair'), before the kit existed as a git repo — so `revision` is the kit's FIRST PUBLISHED commit, not the exact copy point; do not read it as the tree this repo was copied from. Every copied artifact is deliberately adapted for a Deno 2 + Oak + Postgres app; each reason is below and is also written in INCIDENTS.md. The kit's C++ and Python artifacts (tools/ci.sh, .ci.env.example, templates/*/gate.sh for the other language) were never applicable here and are not listed. Rule that keeps this file true: any commit that edits a copied artifact must update that artifact's repo_sha256 in the same commit. **2026-09-20, card t_0cc793fb:** `revision` moved to 951608c: the kit's `kit probes` step was ported into `scripts/gate.sh` (the mechanism for \"a fix that must propagate ships a probe\"). No probe is carried yet — this repo's gate is the deno copy and the kit's first probe guards a C++ gate — so the step SKIPs loudly until one is. Still recorded adapted:true, because the copy is a fork, as before. **2026-09-20, card t_fb62d8fa:** the record gained the kit's per-fix lists (docs/KIT-REVISION-CONVENTION.md, 'Exact file shape'), so what this copy has and has not taken from the kit is machine-readable instead of prose. `declined_fixes` records all three of the kit's probes as NOT APPLICABLE here: they are stages or guards in the kit's templates/cpp/ci.sh, and this repo's gate is the deno one, so there is no second C++ configuration to build and nothing to port -- running them against this gate is what the checker reports as FAILING, and that is the whole reason the record says why. No probe is carried yet, and the gate's kit-probes step SKIPs by design ('carries no probe yet' is not the same statement as 'is behind'). **`revision` and every `kit_sha256` are UNCHANGED by this commit**: nothing here came from the kit, and being behind the kit is not a failure -- the check that called it one was measuring whether the kit had moved. **2026-09-20, card t_0a9a0018:** the kit's `GIT_INDEX_FILE` fix (kit f9c3300) landed in all three kit templates — including this repo's (deno) — and this copy does NOT carry it: `declined_fixes` records it as PARKED with the measurement (the kit's probe FAILS against this gate: the line is absent; nothing fails here today because every git call the gate makes reads its own repo). `revision` and every `kit_sha256` are UNCHANGED: no artifact was taken from the kit in this commit.",
  "declined_fixes": [
    {
      "name": "tidy-baseline",
      "from_revision": "a6ad2ea6951c66eb5b7c2eea8757ab8d3311174e",
      "probe": "probes/tidy-baseline.sh",
      "why": "not applicable: tidy-baseline guards the kit's templates/cpp/ci.sh and this repo's gate is the deno one, so there is no C++ gate here for it to guard -- no stage to port and no decline to weigh."
    },
    {
      "name": "gate-stage-guards",
      "from_revision": "951608c2675a2f5c2d0b59d9c19c632d23a043af",
      "probe": "probes/gate-stage-guards.sh",
      "why": "not applicable: gate-stage-guards guards the kit's templates/cpp/ci.sh and this repo's gate is the deno one, so there is no C++ gate here for it to guard -- no stage to port and no decline to weigh."
    },
    {
      "name": "optimized-build-stage",
      "from_revision": "d531a110184125ed108c3eabc36834bb803404bb",
      "probe": "probes/optimized-stage.sh",
      "why": "not applicable: optimized-build-stage guards the kit's templates/cpp/ci.sh and this repo's gate is the deno one, so there is no C++ gate here for it to guard -- no stage to port and no decline to weigh."
    },
    {
      "name": "git-index-file",
      "from_revision": "f9c33005d880df095d7a5ab300d1ec5349c59aea",
      "probe": "probes/git-index-file.sh",
      "why": "PARKED, not declined on the merits, and the measurement is the reason: the kit's probe FAILS against this gate (deno side) — PROBE FAILED (2 ok, 3 failed) — because `unset GIT_INDEX_FILE` is not in scripts/gate.sh, so a pathspec commit hands git's TEMPORARY index to everything the gate runs. What is NOT here is an instance of the defect: every git call this gate makes reads its own repo (git diff, git ls-files, git show, git rev-parse, git merge-base, git cat-file), there is no `git -C`, no clone and no dependency checkout, so nothing fails today. The fix becomes live the moment a stage ever shells out to something that clones — a `deno` subprocess that fetches, or the first probe this repo carries, since the kit's probes run git in throwaway repositories by design. Adopt it in that sync — one prologue line, the probe copied to tools/kit-probes/git-index-file.sh, and an adopted_fixes entry naming kit f9c3300 — rather than now, when it would be a line with nothing behind it. Unlike the three C++ probes recorded above, this one is NOT 'not applicable': it guards all three templates, and the kit runs it against templates/deno/gate.sh and templates/python/gate.sh too."
    }
  ],
  "files": [
    {
      "repo_path": "scripts/gate.sh",
      "kit_path": "templates/deno/gate.sh",
      "repo_sha256": "be44094a6115169804fbb55e189804999b41c26747bca411c40c7d4a43a5acfe",
      "kit_sha256": "5285e870fe932141c9f0c9831f11b78cfcf5f189ec51be77444d7f0124d235bf",
      "adapted": true,
      "adapted_why": "Deno app, not the kit's public/version.js <-> sw.js pair: artifact-identity checks web/version.js APP_VERSION against the ?v= on every local asset reference in every page (and fails when web/ changes without web/version.js); added an env stage and a 'files this branch touches' helper; format tolerates a file already unformatted at HEAD; lint is absolute, no baseline."
    },
    {
      "repo_path": ".githooks/pre-commit",
      "kit_path": "templates/hooks/pre-commit",
      "repo_sha256": "624fc400146138de42ddfa94d75c7b7d8b57e20eeeca245d88a11afde63a1c40",
      "kit_sha256": "e304ddcb48e216029c8e77402d927695dae4895532f6bf12d1ecb54604b652d8",
      "adapted": true,
      "adapted_why": "Deno-only repo: the C++ branch (exec tools/ci.sh build tests) is deleted, since the whole Deno gate is already seconds and a commit can run all of it."
    },
    {
      "repo_path": ".githooks/pre-push",
      "kit_path": "templates/hooks/pre-push",
      "repo_sha256": "bbdbd4787dfc0d363b84c10a1dc539f61352346cf489ff51eed5b01ac02bd5a4",
      "kit_sha256": "f94958bcd22089f12d3987564ec2965d99577f6c667ee64ded466aa937e9a5eb",
      "adapted": true,
      "adapted_why": "The C++-only paragraph (clean worktree + pristine checkout of HEAD) is deleted; the Deno gate is already the whole gate and carries its own clean-checkout story."
    },
    {
      "repo_path": "INCIDENTS.md",
      "kit_path": "INCIDENTS.md",
      "repo_sha256": "92570926186fd8bb1476ec352e874e3d10588b87201d3df96c483b5ffffe4d5f",
      "kit_sha256": "aef9beb070e319176ef135eed5317ec8caab4bb0192a68a452c9d6841ebd4c45",
      "adapted": true,
      "adapted_why": "Seeded from the kit's rule block on 2026-09-19 and carries this repo's own entries plus a 'Deliberate deviations from the AI-DEV-STARTER gate' section; the kit's own 2026-09-19/20 entries are deliberately NOT copied — this file is per-repo history, so a sync must never overwrite it with the kit's."
    },
    {
      "repo_path": "docs/PROJECT-CONTRACT.md",
      "kit_path": "templates/CLAUDE.md.template",
      "repo_sha256": "782820ed8e5a0fd161437447b4e0758441b3b35fb2fa0c222c30b48b68c72245",
      "kit_sha256": "615ad65a51eb5b70a59882d261eeacc1510cbd7c8cd59d730bba945ad19fa3da",
      "adapted": true,
      "adapted_why": "Filled in for this repo, and named PROJECT-CONTRACT.md rather than CLAUDE.md: CLAUDE.md is a protected agent-instruction name here — creating it opens an approval prompt, and both unattended attempts (2026-09-19, 2026-09-20) were refused, so the contract lives in docs/ until a human runs 'git mv docs/PROJECT-CONTRACT.md CLAUDE.md'."
    }
  ]
}
